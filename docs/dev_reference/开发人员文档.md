# PE-MMNet v4 开发人员文档

> 指导新成员理解代码、修改代码、排查问题

---

## 一、项目概述

### 1.1 物理背景

日用陶瓷在热震过程中产生裂纹，需要预测裂纹的位置、尺寸和密度。

**物理约束**：
- 裂纹密度随温度下降单调递增（格里菲斯断裂准则）
- 裂纹不可逆
- 裂纹位置与应力集中区域相关

### 1.2 输入输出

| 任务模式 | 输入 | 输出 |
|----------|------|------|
| `detection` | 温度时序 + 图像 | [x, y, l, w, conf, density] |
| `segmentation` | 温度时序 + 图像 | 256×256 二值掩膜 |
| `multitask` | 温度时序 + 图像 | 检测向量 + 分割掩膜 |

### 1.3 模型变体

| 变体 | 架构 | 显存占用 | 说明 |
|------|------|----------|------|
| `resnet18` | ResNet-18 + Cross-Attention | ~2GB | 轻量基线，稳定快速 |
| `swin_yolo` | Swin-Tiny + YOLO-FPN + Cross-Attention | ~4GB | 空间网格定位，图像注意力 |
| `vit_yolo` | ViT-Small + YOLO-FPN + Cross-Attention | ~3GB | 全局自注意力，轻量 |
| `swin_yolo_patchtst` | Swin-Tiny + YOLO-FPN + PatchTST | ~4GB | YOLO + PatchTST时序骨干 |
| `detr` | ResNet-18 + Transformer + Cross-Attention | ~5GB | 全局上下文，100个Object Queries |

---

## 二、目录结构

```
project_v4/
├── run_train.py                 # 统一训练入口
├── train_launcher.py           # 交互式启动器
├── team_train.py               # 团队协作脚本
├── launcher.py                 # 命令构建器
├── config.json                 # 数据路径配置
│
├── data/
│   └── dataset_multimodal.py   # 多模态数据加载
│
├── models/
│   ├── pe_tsnet_multimodal.py  # PETSNetMultimodal + 所有基础组件
│   ├── pe_tsnet_yolo.py        # SwinYOLOFPN + ViTYOLOFPN + SwinYOLOFPNWithPatchTST
│   ├── pe_tsnet_detr.py        # DETRStyle
│   ├── pe_tsnet_patchtst.py    # PatchTST 1D骨干
│   └── pe_tsnet_fusion.py     # 门控融合模块
│
├── training/
│   ├── mono_loss.py           # 所有损失函数
│   └── augmentation.py         # 数据增强（ThermalCutMix）
│
├── utils/
│   ├── config.py              # 配置管理
│   └── console.py             # 控制台输出
│
├── tasks/                     # 团队任务配置目录
│   ├── team_baseline.json
│   ├── team_optimization.json
│   └── batch_ablation.json
│
├── checkpoints/               # 模型检查点
├── logs/                     # 训练日志
│   └── team_training.log     # 团队任务执行日志
│
└── docs/                     # 文档
    ├── user_guides/          # 用户指南
    ├── dev_reference/        # 开发参考
    ├── architecture/         # 架构设计
    ├── experiment_reports/   # 实验报告
    ├── collaboration/        # 团队协作
    └── archived/             # 归档文档
```

---

## 三、核心模块

### 3.1 数据模块 (data/dataset_multimodal.py)

**职责**：多模态数据加载，支持多种数据格式

**关键函数**：
- `create_multibatch_dataloaders()` - 创建多批次数据加载器
- `create_triple_channel_seq()` - 生成三通道时序数据

**数据格式**：
```
数据子文件夹/
├── 温度/              # PNG图像（必须）
├── 应力/              # PNG图像（必须）
├── 表格/              # CSV标签（可选）
│   └── *.csv
├── d_hist/            # 分割掩膜（可选）
└── 探针CSV/          # 时序数据（可选）
```

### 3.2 模型模块 (models/)

| 文件 | 关键类 | 说明 |
|------|--------|------|
| `pe_tsnet_multimodal.py` | `PETSNetMultimodal`, `ResNet18Backbone2D`, `CrossAttentionFusion`, `GatedMultimodalFusion` | 基础模型 + 全部融合组件 |
| `pe_tsnet_yolo.py` | `SwinYOLOFPN`, `ViTYOLOFPN`, `SwinYOLOFPNWithPatchTST` | YOLO-FPN 变体 |
| `pe_tsnet_detr.py` | `DETRStyle` | DETR 风格变体 |
| `pe_tsnet_patchtst.py` | `PatchTST1D`, `PatchTSTWithRate` | PatchTST 1D骨干 |
| `pe_tsnet_fusion.py` | `GatedMultimodalFusion`, `AdaptiveFusion` | 历史版本，无外部引用 |

### 3.3 损失函数 (training/mono_loss.py)

| 损失类 | 用途 | 变体 |
|--------|------|------|
| `MultimodalCrackLoss` | 检测任务 | resnet18 |
| `YOLOLoss` | YOLO检测任务 | swin_yolo, vit_yolo, swin_yolo_patchtst |
| `YOLOTargetAssigner` | YOLO目标分配 | swin_yolo, vit_yolo, swin_yolo_patchtst |
| `DETRLoss` | DETR检测任务 | detr |
| `HungarianMatcher` | DETR最优匹配 | detr |
| `SegmentationLoss` | 分割任务 | 所有变体（segmentation模式） |
| `MultimodalSegmentationLoss` | 检测+分割联合 | multitask模式 |
| `DensityConsistencyLoss` | 密度一致性约束 | 可选 |
| `CombinedDensityLoss` | MSE+一致性组合 | 可选 |

### 3.4 训练入口 (run_train.py)

**关键函数**：
- `train_model()` - 主训练循环
- `evaluate_model()` - 模型评估
- `estimate_training_time()` - 动态时间估算
- `freeze_model_backbone()` - 冻结骨干网络

---

## 四、模型架构详解

### 4.1 基础模型：PETSNetMultimodal (resnet18)

```
输入
├── 1D分支：(B, 300) 温度序列
│   ├── Micro1DCNN → 局部特征 (32维)
│   ├── Macro1DCNN → 全局趋势 (32维)
│   ├── Multi-Head Self-Attention → 通道融合
│   └── 输出：64维向量
│
└── 2D分支：(B, 2, H, W) 温度场+应力场
    ├── ResNet-18（ImageNet预训练）
    │   └── 输出：(B, 512, H', W')
    └── GlobalAvgPool → (B, 512) 向量

融合：CrossAttentionFusion
输出：MultiTaskHead → (B, 6) [x, y, l, w, conf, density]
```

### 4.2 SwinYOLOFPN (swin_yolo)

```
输入
├── 1D分支：同 PETSNetMultimodal → 64维
│
└── 2D分支：(B, 2, 512, 512)
    ├── SwinBackbone2D（timm Swin-Tiny，img_size=512）
    │   └── 输出多尺度特征
    ├── yolo_proj：Conv2d(768→256) + BN + ReLU
    └── YOLOFPNHead → (B, 6, 16, 16)

训练模式：返回 (B, 256, 6) 完整网格预测
推理模式：取最高conf → CrossAttentionFusion → MultiTaskHead
```

### 4.3 ViTYOLOFPN (vit_yolo)

```
输入
├── 1D分支：TemporalFeatureExtractor → 64维
│
└── 2D分支：(B, 2, 任意尺寸)
    ├── input_resize：Conv2d(2→3, kernel_size=1) + BN + ReLU
    ├── ViTYOLOBackbone2D（timm vit_small_patch16_224）
    │   └── 输出：(B, 256, 7, 7)（骨干输出已为256通道）
    ├── interp：Resize到 (16, 16)
    ├── fpn：Conv2d(256→256, kernel_size=3) + BN + ReLU
    └── YOLOFPNHead → (B, 256, 6)

训练模式：返回 (B, G², 6) 完整网格预测（G² = actual_grid_size²）
推理模式：取最高conf → fusion → output_head → (B, 6)
```

**actual_grid_size**：通过 `max(16, (image_size//32 // 4) * 4)` 动态计算（至少16）。

### 4.4 SwinYOLOFPNWithPatchTST (swin_yolo_patchtst)

与 SwinYOLOFPN 相同架构，但 1D 分支使用 `PatchTST1D` 替代 `TemporalFeatureExtractor`。支持 triple_channel 三通道输入。

### 4.5 DETRStyle (detr)

```
输入
├── 1D分支：TemporalFeatureExtractor → 64维
│
└── 2D分支：(B, 2, 256, 256)
    ├── ResNet18Backbone2D（启用 set_spatial_output=True）
    │   └── 输出：(B, 512, 8, 8)
    ├── TransformerEncoder（6层）
    ├── DETRDecoder（100个Object Queries）
    └── DETRHead → (B, 100, 6)

训练链路：
  detr_pred (B, 100, 6)
  → Hungarian 匹配选最佳 → best_query (B, 6)
  → query_proj(6→128)
  → fusion(dim_2d=128, dim_1d=64) → (B, 192)
  → output_head(192→6) → final_output (B, 6)
  loss = HungarianLoss + 0.5×SuperviseLoss

推理模式：argmax(conf) → query_proj → fusion → output_head → (B, 6)
```

---

## 五、训练/推理模式设计

**核心原则**：所有新变体（swin_yolo/vit_yolo/detr）统一使用 `self.training` 属性区分模式：

```python
if self.training:
    return raw_pred, global_density  # 用于损失计算
else:
    # 取最佳预测 → 融合 → 输出头
    output = self.output_head(self.fusion(best_pred, feat_1d))
    return output, global_density
```

**关键点**：
- 训练模式：返回原始预测（供损失函数使用）
- 推理模式：经过融合+输出头（用于实际预测）

---

## 六、损失函数选择

| 变体 | 损失函数 | 目标分配 |
|------|----------|----------|
| `resnet18` | `MultimodalCrackLoss` | 直接比较（6维向量L1/L2） |
| `swin_yolo` | `YOLOLoss` | `YOLOTargetAssigner`（网格分配） |
| `vit_yolo` | `YOLOLoss` | `YOLOTargetAssigner`（网格分配） |
| `swin_yolo_patchtst` | `YOLOLoss` | `YOLOTargetAssigner`（网格分配） |
| `detr` | `DETRLoss` | `HungarianMatcher`（最优匹配） |

---

## 七、训练时间估算

### 7.1 动态估算

训练开始前会自动测量2-3个epoch的实际速度：
- 保存/恢复模型状态，不影响训练
- 显示 "预估总时间: ~X小时"

### 7.2 实时 ETA

每10轮显示训练进度盒子：
```
║ System| EMA: 1.3s/epoch | ETA: 2m55s | 完成: 14:32 ±0.2s   ║
```

- **EMA**：指数移动平均，更稳定
- **置信区间**：至少5个epoch后显示
- **预计完成时间**：具体时间点

### 7.3 ETAEstimator 类

```python
from run_train import ETAEstimator

# 初始化
eta = ETAEstimator(total_epochs=150, alpha=0.3)

# 每轮更新
eta.update(epoch_time)

# 获取 ETA 信息
info = eta.get_eta(current_epoch)
# info: {'ema', 'eta_str', 'finish_time', 'confidence', ...}
```

---

## 八、高级优化功能

### 8.1 分阶段训练

```bash
python run_train.py --variant resnet18 --staged_train --epochs 150
```

先短序列预训练，再长序列微调。

### 8.2 注意力模块

```bash
# 坐标注意力（保留位置信息）
python run_train.py --variant swin_yolo --use_coord_attn
```

`BackboneWithAttention` 包装器支持多尺度骨干（返回列表时自动取最后一层）。

### 8.3 门控融合

```bash
python run_train.py --variant resnet18 --fusion gated
```

温度/应力分治策略，将2D特征按通道拆分为温度/应力子空间分别处理，由1D特征门控融合。

### 8.4 三通道输入

```bash
python run_train.py --variant resnet18 --triple_channel
```

使用 [初始温度, 当前温度, 温度变化率] 三通道。

### 8.5 ThermalCutMix

```bash
python run_train.py --variant resnet18 --aug_cutmix_prob 0.3
```

物理安全版，仅作用于温度通道。

---

## 九、团队协作训练系统

### 9.1 系统架构

```
tasks/*.json          # 任务配置（协调者创建）
       ↓
team_train.py         # 自动加载并显示菜单
       ↓
组员选择任务 → run_train.py 执行训练
```

### 9.2 命令行参数

```bash
# 交互式菜单
python team_train.py

# 自动执行
python team_train.py --auto

# 强制执行（包括硬件警告任务）
python team_train.py --auto --force

# 导入检查点
python team_train.py --import
```

### 9.3 检查点完成状态机制

**双保险检查点**：
- `{...}_best.pt`：仅在验证损失改善时更新
- `{...}_last.pt`：每个 epoch 结束无条件保存（崩溃兜底）

**分层目录**：
```
checkpoints/
├── resnet18/          # 按变体分子目录
│   ├── best.pt
│   └── last.pt
├── swin_yolo/
│   └── ...
└── backup/             # 配置变更时自动备份
    └── ...config_changed_时间戳.pt
```

**新命名格式**：`{variant}_{task}_off{offset}_{best|last}.pt`
- 示例：`resnet18_detection_off0_best.pt`

**元数据字段**：
- `is_complete`：`True`=训练正常结束，`False`=意外中断
- `epoch`：已完成的 epoch 数（1-indexed）
- `save_reason`：`improvement`/`epoch_end`/`early_stop`/`completed`
- `config`：完整训练配置（用于签名校验）

### 9.4 配置签名校验

加载检查点时会自动校验 14 个关键配置字段，不匹配时会强制重训并备份旧文件：

| 签名字段 | 说明 |
|----------|------|
| `variant`, `fusion`, `backbone_2d/1d` | 模型架构 |
| `task`, `predict_offset` | 任务模式 |
| `image_size`, `fp16`, `triple_channel` | 输入配置 |
| `use_coord_attn`, `staged_train` | 训练策略 |

> `epochs`、`patience`、`lr` 等运行时参数**不在签名中**，变更不会触发重训。

### 9.5 Epoch 目标数变更

系统支持在不同运行间改变目标 epoch 数：

| 场景 | 行为 |
|------|------|
| 150 epoch 早停在 85，下次想跑 200 | 自动从 85 继续 |
| 跑了 3 epoch 测试，下次想跑 100 | 自动从 3 继续 |
| 跑完了 150 epoch，下次想跑 200 | 自动从 150 继续 |
| 跑完了 150 epoch，只想跑 100 | 直接返回（已达目标） |

### 9.6 检查点迁移工具

每次运行 `run_train.py` 时自动执行旧检查点迁移：
- 旧格式文件 → 自动移动到对应变体子目录
- 队友分享的单文件 → 自动归类到对应变体子目录
- 冲突时 → 旧文件重命名为 `.conflict_时间戳.pt`

**加载行为（决策矩阵）**：

| 检查点状态 | 配置签名 | 目标 Epoch > 已训练? | 行为 |
|------------|----------|---------------------|------|
| ✅ 已完成 | ✅ 一致 | ✅ 是 | 从 saved_epoch 继续训练 |
| ✅ 已完成 | ✅ 一致 | ❌ 否 | 直接返回 |
| ✅ 已完成 | ❌ 不一致 | 任意 | 备份旧文件 + 强制重训 |
| ❌ 未完成 | ✅ 一致 | 任意 | 从断点恢复 |
| ❌ 未完成 | ❌ 不一致 | 任意 | 备份旧文件 + 强制重训 |

> 重新训练请删除检查点文件或使用 `--force_retrain`。

---

## 十、添加新模型变体

### 10.1 步骤

1. 在 `models/` 定义模型类
2. 在 `run_train.py` 的 `VARIANT_MODELS` 注册
3. 在 `launcher.py` 的 `ABLATION_PRESETS` 添加快捷键
4. 在 `models/__init__.py` 导出

### 10.2 示例

```python
# models/pe_tsnet_new.py
class NewVariant(nn.Module):
    def __init__(self, ...):
        super().__init__()
        # 模型定义...

    def forward(self, x_1d, x_2d):
        if self.training:
            return raw_pred, global_density
        else:
            return best_pred, global_density
```

### 10.3 注册到 run_train.py

```python
VARIANT_MODELS = {
    'new_variant': lambda cfg: NewVariant(...),
    # ...
}
```

---

## 十一、添加新损失函数

### 11.1 步骤

1. 在 `training/mono_loss.py` 定义损失类
2. 在 `run_train.py` 的 `train_model()` 中选择

### 11.2 示例

```python
# training/mono_loss.py
class NewLoss(nn.Module):
    def __init__(self, ...):
        super().__init__()
        # 损失定义...

    def forward(self, output, global_density, labels):
        # 计算损失
        return loss_total
```

---

## 十二、常见问题排查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| CUDA: False | PyTorch 未安装 CUDA 版本 | `pip install torch --index-url https://download.pytorch.org/whl/cu118` |
| Loss 是 NaN | 学习率过高 | `--lr 1e-4` |
| 显存不足 | 批次过大 | `--image_size 256 --batch_size 4` |
| R² 为 0 | 图像尺寸不匹配 | 检查 `--image_size` 与训练一致 |
| 未找到数据 | 数据路径错误 | 检查 `config.json` 的 `data_root` |
| ViT 输入尺寸错误 | ViT 只接受 224 | 使用 `input_resize` 层 |
| DETR 特征图尺寸错误 | ResNet 未启用空间输出 | 调用 `set_spatial_output(True)` |
| DETR 位置编码维度错误 | feat_size=16 应为 8 | 已修复（stride=32 → dynamic feat_size） |
| staged_training YOLO 报错 | 模型参数缺失 | 已修复（补充完整参数） |
| GatedMultimodalFusion 精度低 | 输出只返回门控输出，丢失2D特征 | 已修复（改为 torch.cat 拼接） |
| PhysicalSafeTransform1D triple_channel 异常 | seq_len 取 len(seq) 对 (3,300) 返回3 | 已修复（改为 seq.shape[-1]） |
| CoordAtt 注入后报错 | BackboneWithAttention 未开启空间输出 | 已修复（注入前调用 set_spatial_output） |
| ViTYOLO / SwinYOLO-PatchTST 网格不匹配 | actual_grid_size 未动态计算 | 已修复（image_size//32 动态适配） |

---

## 十三、代码规范

### 13.1 命名规范

- 类名：`PascalCase`（如 `SwinYOLOFPN`）
- 函数名：`snake_case`（如 `estimate_training_time`）
- 常量：`UPPER_SNAKE_CASE`（如 `BOX_WIDTH`）

### 13.2 训练/推理模式

- **必须**使用 `self.training` 属性判断模式
- 训练模式返回原始预测供损失计算
- 推理模式返回最优预测

---

## 十四、控制台输出样式

### 颜色函数

| 颜色 | 函数 | 用途 |
|------|------|------|
| 白色加粗 | `print_title()` | 章节标题 |
| 白色加粗 | `print_section()` | 小节标题 |
| 青色 | `print_result()` | 关键指标、评估结果 |
| 青色 | `print_results_table()` | 指标表格输出 |
| 灰色 | `print_info()` | 普通日志 |
| 灰色 | `print_divider()` | 分隔线 |
| 灰色 | `print_metric_row()` | 指标行 |
| 黄色 | `print_warning()` | 警告提示 |
| 红色 | `print_error()` | 错误信息 |
| 绿色 | `print_success()` | 成功提示 |

### 代码使用

```python
from utils.console import (
    print_title,       # 章节标题
    print_section,     # 小节标题
    print_result,      # 关键指标
    print_results_table,  # 指标表格
    print_info,        # 普通信息
    print_warning,     # 警告
    print_error,       # 错误
    print_success,     # 成功
    print_divider,     # 分隔线
)

print_title("开始训练...")
print_result("R2", 0.9399)
print_warning("显存不足")
```

### 训练输出格式

每10轮显示进度盒子：
```
╔════════════════════════════════════════════════════════════════╗
║ Epoch 15/150 | Best: 12 | Progress: [████████░░░░░░░░] 10%  ║
╠════════════════════════════════════════════════════════════════╣
║ Train | Loss: 0.4562 | LR: 3.00e-04 | Time: 1.3s          ║
╠════════════════════════════════════════════════════════════════╣
║ Valid | R2: 0.8756 | RMSE: 0.1423 | 违反率: 38.2%          ║
╠════════════════════════════════════════════════════════════════╣
║ System| EMA: 1.3s/epoch | ETA: 2m55s | 完成: 14:32 ±0.2s   ║
╚════════════════════════════════════════════════════════════════╝
```

盒子说明：
- **标题行**：Epoch、Best、进度条
- **Train行**：Loss、LR、Time、Patience
- **Valid行**：R2、RMSE、MAE、违反率
- **System行**：EMA、ETA、预计完成时间、置信区间

### Windows 兼容性

代码自动处理 Windows ANSI 颜色支持，无需额外配置。

---

## 十五、相关文档

| 文档 | 内容 |
|------|------|
| [README](../../README.md) | 项目概述和快速开始 |
| [快速配置指南](../user_guides/快速配置指南.md) | 环境配置和快速上手 |
| [架构设计文档](../architecture/架构设计文档.md) | 模型架构和数据流 |
| [团队协作训练指南](../collaboration/团队协作训练指南.md) | team_train 使用手册 |
| [API文档](api.md) | 接口参考 |
