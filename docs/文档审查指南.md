# 文档审查指南

> PE-MMNet v4 文档审查通用检查清单 | 每次文档更新后必读

---

## 一、审查时机

| 时机 | 说明 |
|------|------|
| 代码变更后 | 检查涉及文档是否需要同步更新 |
| 新增模型变体/损失函数 | 更新架构设计文档和API文档 |
| 新增命令行参数 | 更新用户指南和开发人员文档 |
| 文档重构后 | 验证所有交叉引用链接正确 |

---

## 二、通用审查维度

### 2.1 准确性审查（最重要）

**核心原则**：文档必须与实际代码一致。

#### 必查项

| 检查项 | 审查方法 |
|--------|----------|
| CLI 参数是否存在 | `grep -n "run_train.py --" docs/` 或在 `run_train.py` 中搜索 |
| 函数签名是否正确 | 对照 `run_train.py` / `data/dataset_multimodal.py` 等源文件 |
| 默认参数值是否正确 | 检查源代码中的默认值 |
| 函数/类名是否正确 | 对照 `models/`, `training/` 源文件 |
| 损失权重默认值是否正确 | 检查 `training/mono_loss.py` 中的默认值 |
| 硬件要求/显存占用是否合理 | 对照代码中的显存检测逻辑 |

#### 常见错误模式

- ❌ 文档写了 `--use_se` 但实际只有 `--use_coord_attn`
- ❌ 文档写了 `patience=20` 但实际默认 `patience=30`
- ❌ 文档写了 `lr=3e-4` 但实际默认 `lr=1e-3`（架构自适应调整为 `1e-4`）
- ❌ ViTYOLOFPN 描述有 `384→256` 投影但实际骨干输出直接是 256 通道
- ❌ 描述了不存在的函数/类

#### 验证命令

```bash
# 验证参数存在
grep -n "use_se\|use_coord" run_train.py

# 验证函数签名
python -c "from run_train import train_model; import inspect; print(inspect.signature(train_model))"

# 验证模型架构
python -c "from models.pe_tsnet_yolo import ViTYOLOFPN; import torch; m = ViTYOLOFPN(); print([(n, p.shape) for n, p in m.named_children()])"
```

---

### 2.2 完整性审查

#### 检查清单

| 文档类型 | 必须包含的内容 |
|----------|--------------|
| 用户指南 | 安装依赖、准备数据、快速开始、常见问题 |
| 开发人员文档 | 项目概述、目录结构、核心模块、代码规范、常见问题 |
| 架构设计文档 | 所有模型变体的数据流、损失函数选择、训练/推理模式 |
| 调参指南 | 所有可调参数、默认值、适用场景 |
| API文档 | 所有公开函数签名、参数说明、返回值 |
| 团队协作指南 | 任务配置格式、依赖管理、检查点导入 |

#### 遗漏检查

- [ ] 新增的模型变体是否在架构文档和变体选择表中
- [ ] 新增的命令行参数是否在用户指南和调参文档中
- [ ] 新增的损失函数是否在架构文档中说明
- [ ] 新增的工具函数是否在API文档中记录

---

### 2.3 一致性审查

#### 跨文档一致性

同一信息在多个文档中出现时，必须保持一致：

| 必查一致性项 | 涉及文档 |
|------------|----------|
| 默认 patience 值 | 快速配置指南、调参与算法工程指导文档、项目算法报告 |
| 默认学习率 | 用户指南、调参文档、开发人员文档 |
| 模型显存占用 | 架构设计文档、用户指南、团队协作指南 |
| 模型输入尺寸 | 架构设计文档、用户指南 |
| 任务ID命名 | 团队协作指南、开发人员文档 |
| 损失函数默认值 | 架构设计文档、调参文档 |

#### 交叉引用检查

```bash
# 验证所有 .md 链接
grep -rn "]\(" docs/ --include="*.md"

# 检查是否引用了不存在的文件
for f in docs/**/*.md; do
  links=$(grep -oP '\]\([^)]+\.md\)' "$f" | sed 's/.*\](\([^)]*\))/\1/')
  for link in $links; do
    if [[ ! -f "docs/$link" && ! -f "$(dirname $f)/$link" ]]; then
      echo "$f: broken link → $link"
    fi
  done
done
```

---

### 2.4 时效性审查

#### 检查项

| 问题 | 说明 |
|------|------|
| 过时命令 | 命令格式是否与当前代码一致（如 `--mode train` vs `--mode`） |
| 过时文件路径 | 代码文件是否已移动或重命名 |
| 过时架构描述 | 模型架构是否与实际代码一致 |
| 过时超参数 | 学习率、patience 等默认值的描述是否最新 |

---

### 2.5 可读性审查

| 检查项 | 说明 |
|--------|------|
| 结构清晰 | 章节标题是否准确反映内容 |
| 示例完整 | 命令示例是否可独立运行 |
| 术语统一 | 全文档使用同一术语（如"时序"而非混用"时间序列"） |
| 表格格式 | 表格是否对齐，是否包含必要列 |

---

### 2.6 代码示例审查

| 检查项 | 方法 |
|--------|------|
| 代码语法正确 | 用 Python 执行示例片段 |
| 参数名正确 | 与源文件对照检查 |
| 示例可运行 | 复制到命令行执行 |
| 示例不过时 | 确认命令格式与当前代码一致 |

---

## 三、按文档类型的审查要点

### 3.1 用户指南（快速配置指南.md）

| 审查要点 | 正确性标准 |
|----------|-----------|
| 所有模型变体命令格式 | 每个变体至少有1个可运行的训练命令 |
| 显存配置表 | 与 `run_train.py` 中的 `get_memory_config()` 一致 |
| 预训练权重说明 | 说明需要 HuggingFace 权重（swin_yolo/vit_yolo/detr） |
| 镜像站设置 | `HF_ENDPOINT=https://hf-mirror.com` 是否在说明中 |
| 常见问题表 | 与 `run_train.py` 中的实际错误信息一致 |

### 3.2 开发人员文档（开发人员文档.md）

| 审查要点 | 正确性标准 |
|----------|-----------|
| 目录结构 | 与实际 `project_v4/` 目录一致 |
| 模型架构图 | 与 `models/` 中实际类定义一致 |
| 训练/推理模式设计 | 与代码中 `self.training` 的使用一致 |
| 损失函数选择表 | 与 `run_train.py` 中的实际选择逻辑一致 |
| 常见问题表 | 与实际错误信息一致，解决方案可行 |
| 控制台输出样式 | 与 `utils/console.py` 中的实际函数一致 |

### 3.3 架构设计文档（架构设计文档.md）

| 审查要点 | 正确性标准 |
|----------|-----------|
| 所有5个模型变体 | `resnet18`/`swin_yolo`/`vit_yolo`/`detr`/`swin_yolo_patchtst` 全部覆盖 |
| 模型输入输出维度 | 每个变体都有 (B, ?, ?) 维度的精确描述 |
| 训练/推理模式设计 | 训练返回原始预测，推理返回最优预测 |
| 损失函数选择 | 与代码中的实际损失函数类一致 |
| 显存占用 | 与代码中的实际测试结果一致 |

### 3.4 API文档（api.md）

| 审查要点 | 正确性标准 |
|----------|-----------|
| 所有公开函数 | 包括 `run_train.py`、`data/dataset_multimodal.py`、`training/mono_loss.py` 中的公开函数 |
| 函数签名 | 参数名、默认值、类型注解全部与源文件一致 |
| 返回值描述 | 与实际代码中的返回值一致 |
| 示例代码 | 可运行，参数值合理 |
| 未文档化函数 | 检查源文件是否有遗漏 |

### 3.5 调参指南（调参与算法工程指导文档.md）

| 审查要点 | 正确性标准 |
|----------|-----------|
| 所有可调参数 | 与 `run_train.py` 中的 argparse 参数一致 |
| 默认值 | 与 argparse 中的 `default` 一致 |
| 损失权重名称 | 与 `training/mono_loss.py` 中的参数名一致 |
| 学习率调整建议 | 与 `get_arch_specific_config()` 中的实际逻辑一致 |
| 调参场景建议 | 与实际评估指标（R²/RMSE/mIoU/违反率）一致 |

### 3.6 团队协作指南（团队协作指南.md）

| 审查要点 | 正确性标准 |
|----------|-----------|
| 任务ID | 与 `team_train.py` 中的内置任务ID一致 |
| JSON配置格式 | 与 `tasks/*.json` 实际格式一致 |
| 硬件等级定义 | 与 `team_train.py` 中的等级检测逻辑一致 |
| 命令行参数 | 与 `team_train.py` 的实际参数一致 |
| 检查点识别逻辑 | 与代码中的任务ID识别方式一致 |

---

## 四、审查流程

### Step 1：代码审查

修改代码后，先确认需要更新的文档：

1. 新增/修改了 CLI 参数 → 更新用户指南、调参文档、开发人员文档
2. 新增/修改了模型架构 → 更新架构设计文档
3. 新增/修改了损失函数 → 更新架构设计文档、调参文档
4. 新增/修改了公开函数 → 更新API文档
5. 新增/修改了团队协作功能 → 更新团队协作指南

### Step 2：文档自检

按照本指南"二、通用审查维度"逐项检查。

### Step 3：交叉引用验证

```bash
# 检查所有 .md 文件中的链接
python -c "
import os, re
docs_dir = 'docs'
for root, dirs, files in os.walk(docs_dir):
    for f in files:
        if f.endswith('.md'):
            path = os.path.join(root, f)
            with open(path, 'r', encoding='utf-8') as fp:
                content = fp.read()
            # 找所有 .md 链接
            links = re.findall(r'\[.+?\]\((.+?\.md)\)', content)
            for link in links:
                # 解析相对路径
                base = os.path.dirname(path)
                target = os.path.normpath(os.path.join(base, link))
                if not os.path.exists(target):
                    print(f'BROKEN: {path} -> {link}')
"
```

### Step 4：运行示例

```bash
# 用户指南命令示例
python run_train.py --mode train --variant resnet18 --epochs 1

# API 文档函数验证
python -c "
from data.dataset_multimodal import create_multibatch_dataloaders
import inspect
sig = inspect.signature(create_multibatch_dataloaders)
print(sig)
"
```

---

## 五、常见问题速查

| 文档问题 | 根因 | 解决方案 |
|----------|------|----------|
| 参数不存在 | 文档基于旧版代码 | 对照 `run_train.py` 重新确认 |
| 默认值不对 | 未同步最新默认值 | 从源代码读取实际默认值 |
| 链接断裂 | 文件重命名后未更新引用 | 用 grep 搜索旧文件名并替换 |
| 架构描述不一致 | 不同文档由不同人维护 | 以架构设计文档为准，其他文档引用 |
| 缺少新增内容 | 审查时未对照源代码 | 必须阅读源代码后再审查 |
