# PE-MMNet v3 多模态裂纹预测模型 依赖
# Python >= 3.8

# PyTorch (GPU 支持需要 cu130)
# 安装 GPU 版本: pip install torch --index-url https://download.pytorch.org/whl/cu130
# 安装 CPU 版本: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# torchvision：用于 ResNet-18 视觉骨干网络
# 安装: pip install torchvision
torchvision>=0.15.0

# 数值计算
numpy>=1.19.0
scipy>=1.5.0

# 图像处理：用于连通域分析提取裂纹边界框
# 安装: pip install scikit-image
scikit-image>=0.19.0

# 机器学习
scikit-learn>=0.23.0

# 数据处理
pandas>=1.3.0

# 可视化与进度
tqdm>=4.60.0
matplotlib>=3.3.0
plotly>=5.18.0

# Pillow：用于图像加载和处理
Pillow>=9.0.0
