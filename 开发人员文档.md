# PE-MMNet v4 开发人员文档

> 指导新成员理解代码、修改代码、排查问题

---

## 1. 项目概述

### 1.1 问题背景

日用陶瓷在热震过程中产生裂纹，需要预测裂纹的位置、尺寸和密度。

**物理约束**：
- 裂纹密度随温度下降单调递增（格里菲斯断裂准则）
- 裂纹不可逆
- 裂纹位置与应力集中区域相关

### 1.2 输入输出

**输入**：
- 模态 A（1D）：300 点温度时序
- 模态 B（2D）：256×256 双通道图像（温度场+应力场）

**输出**（根据 `--task` 参数）：

| task | 输出 | 说明 |
|------|------|------|
| `detection` | `[x, y, l, w, conf, density]` | 6 维向量 |
| `segmentation` | `(batch, 1, 256, 256)` | 二值掩膜 |
| `multitask` | `(mask, detection)` | 两者 |

---

## 2. 目录结构

```
project_v4/
├── train_launcher.py           # 统一入口（推荐）⭐
├── run_train.py                # 底层训练脚本
├── launcher.py                 # 单次训练逻辑
├── config.json                 # 路径配置
│
├── data/
│   └── dataset_multimodal.py   # 数据加载
│
├── models/
│   └── pe_tsnet_multimodal.py  # 模型定义
│
├── training/
│   └── mono_loss.py            # 损失函数
│
├── tools/
│   ├── batch_train_gui.py      # 批量训练
│   ├── streamlit_app.py        # 可视化工具
│   └── structure_analyzer.py   # 裂纹分析
│
├── utils/
│   ├── config.py               # 配置管理
│   └── console.py              # 控制台输出
│
└── docs/                       # 文档
```

---

## 3. 模块职责

| 模块 | 职责 | 关键函数 |
|------|------|----------|
| `utils/config.py` | 路径配置 | `load_config`, `get_data_root` |
| `utils/console.py` | 彩色输出 | `print_title`, `print_result` |
| `data/` | 数据加载 | `create_multibatch_dataloaders` |
| `models/` | 模型定义 | `PETSNetMultimodal` |
| `training/` | 损失函数 | `MultimodalCrackLoss` |
| `run_train.py` | 训练入口 | `train_model`, `evaluate_model` |

---

## 4. 核心函数详解

### 4.1 run_train.py

```python
# 显存自适应配置
config = auto_select_config(args)
# args: argparse 参数
# 返回: {'image_size': 512, 'batch_size': 8, 'fp16': True}

# 训练
model, metrics = train_model(model, train_loader, test_loader, config, device)

# 评估
metrics = evaluate_model(model, device, task='detection')
# 返回: {'r2': 0.9, 'rmse': 0.05, 'mIoU': 0.6, ...}
```

### 4.2 launcher.py

```python
# 构建命令
cmd = build_command(['--backbone_2d', 'resnet18', '--epochs', '100'])

# 交互式配置
args_list = interactive_mode()

# 队列配置向导
queue_config_wizard(cli)
```

### 4.3 batch_train_gui.py

```python
# 创建命令
cmd = TrainingCommand.from_quick_format('resnet18,100,0,detection')

# 批量执行
trainer = BatchTrainer()
trainer.add_command(cmd)
trainer.execute_all()
```

---

## 5. 模型架构

### 5.1 完整模型 (PETSNetMultimodal)

```
输入
├── 1D 分支: (batch, 300) → Micro+Macro CNN → Self-Attention → 64维
└── 2D 分支: (batch, 2, 512, 512) → ResNet-18 → 512维

融合: Cross-Attention → 576维
输出: MLP → [x, y, l, w, conf, density]
```

### 5.2 模型变体

| 变体 | 说明 |
|------|------|
| `full` | 完整双分支+Cross-Attention |
| `1d_only` | 仅 1D 分支 |
| `2d_only` | 仅 2D 分支 |
| `concat` | 双分支拼接 |
| `add` | 双分支加法 |
| `cross_attn` | Cross-Attention 无残差 |

---

## 6. 损失函数

```python
# 总损失
L_total = λ_mse·L_mse + λ_mono·L_mono + λ_loc·L_loc + λ_conf·L_conf

# 默认权重
criterion = MultimodalCrackLoss(
    lambda_mse_density=1.0,  # 密度预测
    lambda_mono=0.1,         # 单调性约束
    lambda_loc=1.0,          # 定位精度
    lambda_conf=1.0          # 置信度
)
```

---

## 7. 数据流

```
config.json → get_data_root() → get_data_batches()
                                      ↓
                              create_multibatch_dataloaders()
                                      ↓
                              train_loader, test_loader
                                      ↓
                              train_model() → evaluate_model()
                                      ↓
                              checkpoints/*.pt
```

---

## 8. 检查点管理

### 8.1 命名规范

```
{variant}_{backbone_2d}_{backbone_1d}_{fusion}_task{task}_offset{offset}_best.pt
```

示例：
- `full_resnet18_cnn_attn_cross_attn_taskdetection_offset0_best.pt`
- `1d_only_resnet18_cnn_attn_cross_attn_taskdetection_offset0_best.pt`

### 8.2 加载/保存

```python
# 保存
torch.save({
    'model_state_dict': model.state_dict(),
    'config': {...},
    'epoch': epoch,
    'best_loss': best_loss,
}, save_path)

# 加载
checkpoint = torch.load(save_path)
model.load_state_dict(checkpoint['model_state_dict'])
```

---

## 9. 常见问题排查

### 9.1 训练问题

| 问题 | 原因 | 解决 |
|------|------|------|
| CUDA: False | 未安装 GPU 版 PyTorch | `pip install torch --index-url https://download.pytorch.org/whl/cu118` |
| 训练 loss 是 NaN | 学习率过高 | `--lr 1e-4` |
| 显存不足 | batch_size 过大 | `--image_size 256 --batch_size 4` |
| "未找到数据批次" | config.json 路径错误 | 检查 `data_root` |

### 9.2 评估问题

| 问题 | 原因 | 解决 |
|------|------|------|
| R² 为 0 | 评估配置不匹配 | 检查 `--image_size` 是否与训练一致 |
| 检查点加载失败 | 模型结构变化 | 使用 `--force_retrain` 重新训练 |

---

## 10. 开发规范

### 10.1 添加新模型变体

1. 在 `models/pe_tsnet_multimodal.py` 定义模型类
2. 在 `run_train.py` 的 `VARIANT_MODELS` 字典添加条目
3. 在 `launcher.py` 的 `ABLATION_PRESETS` 添加预设

### 10.2 添加新损失函数

1. 在 `training/mono_loss.py` 定义损失类
2. 在 `run_train.py` 的 `train_model()` 中根据 `task` 选择损失函数

### 10.3 添加新数据批次

1. 将数据放入数据根目录
2. 运行 `python train_launcher.py` 重新配置，或手动编辑 `config.json`

---

## 11. 测试命令

```bash
# 单次训练
python run_train.py --mode train --epochs 10

# 评估
python run_train.py --mode eval --checkpoint ./checkpoints/xxx.pt

# 消融实验
python run_train.py --mode ablation --epochs 10

# 批量训练
python train_launcher.py --quick "resnet18,10,0,detection"
```

---

## 12. 相关文档

| 文档 | 内容 |
|------|------|
| [README](../README.md) | 项目概述 |
| [快速配置指南](docs/快速配置指南.md) | 环境配置 |
| [架构设计文档](docs/架构设计文档.md) | 代码架构 |
| [API文档](api.md) | 接口参考 |
| [调参文档](docs/调参与算法工程指导文档.md) | 调参建议 |
| [算法报告](docs/项目算法与训练实验设计报告.md) | 算法原理 |
