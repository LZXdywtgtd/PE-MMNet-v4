# PE-MMNet v4 开发人员文档

> 指导新成员理解代码、修改代码、排查问题

---

## 1. 项目概述

### 物理背景

日用陶瓷在热震过程中产生裂纹，需要预测裂纹的位置、尺寸和密度。

**物理约束**：
- 裂纹密度随温度下降单调递增（格里菲斯断裂准则）
- 裂纹不可逆
- 裂纹位置与应力集中区域相关

### 输入输出

| 任务 | 输入 | 输出 |
|------|------|------|
| `detection` | 温度时序 + 图像 | [x, y, l, w, conf, density] |
| `segmentation` | 温度时序 + 图像 | 二值掩膜 |
| `multitask` | 温度时序 + 图像 | 检测 + 分割 |

---

## 2. 目录结构

```
project_v4/
├── run_train.py              # 统一训练入口
├── train_launcher.py        # 交互式启动器
├── team_train.py             # 团队协作脚本
├── launcher.py               # 命令构建器
│
├── data/
│   └── dataset_multimodal.py # 数据加载
│
├── models/
│   ├── pe_tsnet_multimodal.py   # 基础模型 + SE/CoordAtt
│   ├── pe_tsnet_yolo.py         # YOLO-FPN变体
│   ├── pe_tsnet_detr.py         # DETR风格变体
│   ├── pe_tsnet_patchtst.py     # PatchTST骨干
│   └── pe_tsnet_fusion.py       # 门控融合
│
├── training/
│   ├── mono_loss.py             # 损失函数
│   └── augmentation.py          # ThermalCutMix
│
├── tools/
│   ├── batch_train_gui.py       # 批量训练GUI
│   └── streamlit_app.py         # 可视化工具
│
├── utils/
│   ├── config.py                # 配置管理
│   └── console.py               # 控制台输出
│
├── tasks/                       # 任务配置目录
│   ├── team_baseline.json
│   ├── team_optimization.json
│   └── batch_ablation.json
│
└── logs/                        # 日志目录
    └── team_training.log        # 团队任务执行日志
```

---

## 3. 核心模块

| 模块 | 职责 | 关键函数 |
|------|------|----------|
| `data/dataset_multimodal.py` | 数据加载 | `create_multibatch_dataloaders` |
| `models/` | 模型定义 | 各变体模型类 |
| `training/mono_loss.py` | 损失函数 | `MultimodalCrackLoss`, `YOLOLoss` |
| `run_train.py` | 训练入口 | `train_model`, `evaluate_model`, `ETAEstimator` |

---

## 4. 模型架构

### 4.1 基础模型 (PETSNetMultimodal)

```
输入
├── 1D: (B, 300) → Micro+Macro CNN → Self-Attention → 64维
└── 2D: (B, 2, H, W) → ResNet-18 → 512维

融合: Cross-Attention → 输出Head
```

### 4.2 模型变体

| 变体 | 架构 | 说明 | 显存 |
|------|------|------|------|
| `resnet18` | ResNet-18 + Cross-Attn | 默认基线 | ~2GB |
| `swin_yolo` | Swin-Tiny + FPN + YOLO | 空间定位 | ~4GB |
| `vit_yolo` | ViT-Small + FPN + YOLO | 全局注意力 | ~3GB |
| `detr` | ResNet-18 + Transformer | 全局感知 | ~5GB |

### 4.3 训练/推理模式

使用 `model.train()` / `model.eval()` 自动切换：
- 训练模式：返回完整预测
- 推理模式：返回最优预测

---

## 5. 损失函数

| 变体 | 损失函数 | 目标分配 |
|------|----------|----------|
| `resnet18` 等 | `MultimodalCrackLoss` | 直接比较 |
| `swin_yolo` | `YOLOLoss` | `YOLOTargetAssigner` |
| `vit_yolo` | `YOLOLoss` | `YOLOTargetAssigner` |
| `detr` | `DETRLoss` | `HungarianMatcher` |

---

## 6. 数据格式

### 数据目录结构

```
数据子文件夹/
├── 温度/              # PNG图像（必须）
├── 应力/              # PNG图像（必须）
├── 表格/              # CSV标签（可选）
│   └── *.csv
├── d_hist/            # 分割掩膜（可选）
└── 探针CSV/          # 时序数据（可选）
```

### 无CSV时的默认行为

- 标签：x=0.01, y=0.01, density=0
- 温度：1400°C恒定

---

## 7. 训练时间估算

### 7.1 动态估算

训练开始前会自动测量2个epoch的实际速度：
- 保存/恢复模型状态，不影响训练
- 显示 "预估总时间: ~X小时"

### 7.2 实时 ETA

每10轮显示训练进度盒子：
```
║ System| EMA: 1.3s/epoch | ETA: 2m55s | 完成: 14:32 ±0.2s   ║
```

- **EMA**：指数移动平均，更稳定
- **置信区间**：至少5个epoch后显示
- **预计完成时间**：具体时间点

### 7.3 ETAEstimator 类

```python
from run_train import ETAEstimator

# 初始化
eta = ETAEstimator(total_epochs=150, alpha=0.3)

# 每轮更新
eta.update(epoch_time)

# 获取 ETA 信息
info = eta.get_eta(current_epoch)
# info: {'ema', 'eta_str', 'finish_time', 'confidence', ...}
```

---

## 8. 高级优化功能

### 分阶段训练 `--staged_train`
```bash
python run_train.py --staged_train --epochs 100
```

### 注意力模块 `--use_coord_attn`
```bash
python run_train.py --use_coord_attn --epochs 100
```

### 门控融合 `--fusion gated`
```bash
python run_train.py --fusion gated --epochs 100
```

### 三通道输入 `--triple_channel`
```bash
python run_train.py --triple_channel --epochs 100
```

### ThermalCutMix `--aug_cutmix_prob`
```bash
python run_train.py --aug_cutmix_prob 0.5 --epochs 100
```

---

## 9. 团队协作训练系统

### 9.1 系统架构

`team_train.py` 是配置驱动的任务分发系统：

```
tasks/*.json          # 任务配置（协调者创建）
       ↓
team_train.py         # 自动加载并显示菜单
       ↓
组员选择任务 → run_train.py 执行训练
```

### 9.2 命令行参数

```bash
# 交互式菜单
python team_train.py

# 自动执行
python team_train.py --auto

# 强制执行（包括硬件警告任务）
python team_train.py --auto --force

# 导入检查点
python team_train.py --import
```

### 9.3 任务执行日志

`logs/team_training.log` 记录每个任务的执行情况：
- 任务ID、状态、耗时、错误信息
- 支持任务追溯和团队协作追踪

### 9.4 检查点元数据

保存检查点时自动记录：
- `task_id`：任务标识
- `epoch`：最佳轮次
- `timestamp`：保存时间

---

## 10. 开发规范

### 添加新模型变体

1. 在 `models/` 定义模型类
2. 在 `run_train.py` 的 `VARIANT_MODELS` 注册
3. 在 `launcher.py` 的 `ABLATION_PRESETS` 添加快捷键

### 添加新损失函数

1. 在 `training/mono_loss.py` 定义损失类
2. 在 `run_train.py` 的 `train_model()` 中选择

---

## 11. 常见问题

| 问题 | 解决 |
|------|------|
| CUDA: False | `pip install torch --index-url https://download.pytorch.org/whl/cu118` |
| Loss 是 NaN | `--lr 1e-4` |
| 显存不足 | `--image_size 256 --batch_size 4` |
| R² 为 0 | 检查 `--image_size` 与训练一致 |
| 未找到数据 | 检查 `config.json` 的 `data_root` |

---

## 12. 相关文档

| 文档 | 内容 |
|------|------|
| [README](README.md) | 项目概述 |
| [API文档](api.md) | 接口参考 |
| [快速配置指南](docs/快速配置指南.md) | 环境配置 |
| [团队协作训练指南](docs/团队协作训练指南.md) | team_train 使用 |
