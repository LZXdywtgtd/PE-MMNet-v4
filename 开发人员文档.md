# PE-MMNet v4 开发人员文档

> **详细程度：能指导新成员理解代码、修改代码、排查问题**

---

## 1. 项目概述与设计动机

### 1.1 问题背景

日用陶瓷在烧制过程中经历快速降温（如从 800°C 降至室温），产生热应力。当热应力超过材料的抗拉强度时，会在陶瓷表面萌生裂纹。裂纹的萌生位置（x, y）、尺寸（长度 l、宽度 w）、是否可见（confidence）、损伤程度（density）是质量检测的核心指标。

传统方法依赖人工目视检测，效率低、漏检率高。本项目通过 COMSOL 仿真获取陶瓷表面温度场和应力场分布，结合机器学习实现裂纹的自动预测。

### 1.2 设计目标

| 目标 | 说明 |
|------|------|
| 多模态融合 | 同时利用 1D 高频温度时序（10Hz，300 点）和 2D 温度/应力场图像（默认 512×512，支持 256/384/768/1024） |
| 物理一致性 | 裂纹密度随时间单调递增（格里菲斯断裂准则），损失函数显式约束 |
| YOLO 式输出 | 一次性输出 6 维目标值，而非两阶段（检测+分类） |
| 多批次支持 | 支持合并多个数据源进行训练 |
| 轻量化部署 | 模型约 12M 参数，推理速度满足在线需求 |

### 1.3 输入输出规格

**输入**：
- 模态 A（1D）：300 点温度时序，来源于 Table_Temp.csv 高频探针数据（706 点插值到 300 点）
- 模态 B（2D）：2 通道图像（温度场 + 应力场），每通道支持多种分辨率（默认 512×512，可选 256/384/768/1024）

**输出**（根据 `--task` 参数）：

| task 参数 | 输出格式 | 说明 |
|-----------|----------|------|
| `detection` | `[x, y, l, w, confidence, density]` | 6 维向量，矩形框检测 |
| `segmentation` | `(batch, 1, image_size, image_size)` 二值掩膜 | 像素级裂纹分割（尺寸由 --image_size 指定） |
| `multitask` | `(mask, detection)` 元组 | 同时输出分割掩膜和检测向量 |

**Detection 输出详解**：
```
[x, y, l, w, confidence, density]
```
- x, y：裂纹中心坐标（归一化到 [0,1]，物理尺寸 0.02m × 0.015m）
- l, w：裂纹长宽（归一化，物理公式 l = √density × 0.3）
- confidence：裂纹存在置信度（Sigmoid 输出，>0.5 表示有裂纹）
- density：裂纹密度（归一化到 [0,1]）

**Segmentation 输出详解**：
- 输出：image_size×image_size 二值掩膜，值域 [0, 1]（默认 512，可通过 `--image_size` 参数调整）
- 标签来源：`d_hist/*.png` 损伤场图像（无需表格数据）

---

## 2. 目录结构与模块职责

```
project_v4/
├── train_launcher.py              # 统一训练入口（单次/批量）⭐推荐使用
├── run_train.py                   # 底层训练脚本（直接训练/消融/评估）
├── launcher.py                    # 单次训练逻辑（被train_launcher.py调用）
├── config.json                    # 路径配置文件（自动生成）
├── requirements.txt               # 依赖列表
├── utils/
│   ├── config.py                  # 配置中心（路径管理）
│   └── console.py                 # 彩色控制台输出
├── data/
│   └── dataset_multimodal.py      # 数据加载、预处理、增强（支持多批次+时间偏移）
├── models/
│   └── pe_tsnet_multimodal.py     # 双分支融合网络
├── training/
│   └── mono_loss.py               # 物理约束损失函数
├── tools/                         # 可视化与分析工具
│   ├── batch_train_gui.py         # 批量训练GUI（被train_launcher.py调用）
│   ├── streamlit_app.py          # Streamlit 交互式分析工具
│   ├── visualization.py           # 命令行可视化工具
│   └── structure_analyzer.py      # 裂纹/气孔分离模块
└── docs/                          # 文档目录
    ├── 快速配置指南.md            # 首次使用必读
    ├── CUDA安装指南.md            # GPU环境配置
    ├── 项目算法与训练实验设计报告.md
    └── 调参与算法工程指导文档.md
```

| 模块 | 职责 | 关键类/函数 |
|------|------|-------------|
| `utils/config.py` | 路径配置管理 | `load_config`、`get_data_root`、`get_data_batches`、`ensure_config` |
| `utils/console.py` | 标准化彩色输出 | `print_title`、`print_result`、`print_results_table` |
| `data/` | CSV/PNG 读取、插值、增强、数据集封装、多批次管理 | `MultiBatchDataset`、`SingleBatchDataset`、`ImagePreprocessor`、`parse_label_csv` |
| `models/` | 2D ResNet-18、1D CNN、Cross-Attention、MLP | `PETSNetMultimodal`、`CrossAttentionFusion`、`TemporalFeatureExtractor` |
| `training/` | 4 分量损失函数（单调性、定位、置信度、MSE） | `MultimodalCrackLoss`、`MonotonicityLossV3`、`verify_monotonicity` |
| `run_train.py` | 统一训练入口 | `train_model`、`train_variant`、`evaluate_model`、`eval_checkpoint` |
| `tools/` | 可视化与分析工具 | `separate_crack_pore`、`auto_optimize_params`、`detect_substrate_bbox` |

---

## 3. 可视化工具详解

### 3.1 Streamlit 交互式工具 (streamlit_app.py)

启动命令：
```bash
streamlit run tools/streamlit_app.py
```

**核心功能**：

1. **实时参数调整**
   - 裂纹阈值：低于此值为裂纹候选（d_hist 中暗色区域）
   - 气孔阈值：高于此值为气孔候选（d_hist 中亮色区域）
   - 合并距离：相邻裂纹碎片合并的距离阈值
   - 最小面积：裂纹最小面积（像素）
   - 边界排除：边缘区域宽度，该区域像素不参与检测

2. **三种视图模式**
   - 二值掩膜：传统黑白掩膜显示
   - 结构分解：裂纹(红色) + 气孔(蓝色) 分离显示
   - 对比视图：原始掩膜与分解结果对比

3. **像素级诊断面板**
   - 输入坐标查看该点是否被识别为裂纹/气孔
   - 使用 Plotly 实现悬停查看坐标和灰度
   - 十字高亮标记显示选中像素位置

4. **裁剪区域可视化**
   - 自动检测基板边界（基于灰度分布）
   - 显示裁剪框（绿色框 + 蓝色角标）
   - 可调节裁剪比例滑块

### 3.2 结构分析模块 (structure_analyzer.py)

**核心函数**：

```python
from tools.structure_analyzer import separate_crack_pore, auto_optimize_params

# 分离裂纹和气孔
crack_mask, pore_mask, overlay, stats = separate_crack_pore(
    img,                           # 归一化后的 d_hist 图像 (H, W)
    crack_threshold=0.2,          # 裂纹阈值
    pore_threshold=0.7,            # 气孔阈值
    crack_min_area=50,            # 裂纹最小面积
    crack_min_aspect_ratio=2.0,   # 裂纹最小长宽比
    merge_distance=10,            # 相邻碎片合并距离
    edge_exclusion=25              # 边界排除宽度
)

# 自动寻优最佳参数
result = auto_optimize_params(img)
```

**算法流程**：
1. 多级阈值分割：裂纹用低阈值+反向，气孔用高阈值
2. 形态学膨胀合并相邻碎片
3. 连通域分析：根据面积和长宽比分离裂纹和气孔
4. 边界伪影过滤：排除接触边缘的扁平区域

### 3.3 图像预处理 (ImagePreprocessor)

```python
from data.dataset_multimodal import ImagePreprocessor

# 默认 256x256
proc = ImagePreprocessor(image_size=256, crop_ratio=0.7)
tensor = proc(pil_image)  # 返回 (1, H, W) tensor

# 高分辨率（支持 384/512/768/1024）
proc_hires = ImagePreprocessor(image_size=512, crop_ratio=0.7)
```

**处理流程**：
1. 灰度转换
2. Max-Min 归一化
3. Center Crop（以基板中心为基准，确保正方形输出）
4. Resize 到目标尺寸

---

## 4. 数据流详解

### 3.1 多批次数据架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                     create_multibatch_dataloaders()                  │
│                                                                      │
│  输入: data_roots = [config.json 中 data_root + 各批次名称]          │
│        获取方式:                                                    │
│          from utils.config import get_data_root, get_data_batches   │
│          data_root = get_data_root()                               │
│          batches = get_data_batches()                               │
│          data_roots = [os.path.join(data_root, b) for b in batches]│
│                                                                      │
│  输出: (train_loader, test_loader)                                  │
│         ├── 训练集: 264 样本 (80%)                                   │
│         └── 测试集: 67 样本 (20%)                                    │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     MultiBatchCollateDataset                         │
│                                                                      │
│  1. 遍历每个数据目录，解析CSV获取样本数                               │
│  2. 合并所有样本信息，统一随机划分                                    │
│  3. 按目录分组，创建 SingleBatchDataset                              │
│  4. 使用 ConcatDataset 合并                                          │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       SingleBatchDataset                             │
│                                                                      │
│  为每个批次独立加载:                                                 │
│  - 标签数据 (Table_表面最大值.csv)                                   │
│  - 探针数据 (Table_Temp.csv)                                        │
│  - 图像数据 (温度/应力/*.png)                                        │
│                                                                      │
│  预处理流程:                                                         │
│  1. ImagePreprocessor: 灰度 → Max-Min归一化 → CenterCrop → Resize   │
│  2. 1D序列: interpolate_seq → PhysicalSafeTransform1D               │
│  3. 2D图像: PhysicalSafeTransform2D                                  │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 数据源文件结构

```
数据根目录/               # config.json 中的 data_root 路径
├── 单次扫描/
│   ├── 温度/温度01.png ~ 温度21.png   # 21张PNG
│   ├── 应力/应力01.png ~ 应力21.png
│   ├── d/d01.png ~ d21.png            # 损伤场
│   └── 表格/
│       ├── Table_表面最大值.csv        # 主标签（21行）
│       └── Table_Temp.csv             # 探针数据
│
├── 参数化扫描1/
│   ├── 温度/温度001.png ~ 温度155.png  # 155张PNG
│   ├── 应力/应力001.png ~ 应力155.png
│   ├── d/d001.png ~ d155.png
│   └── 表格/
│       ├── 参数化扫描-表面最大值.csv   # 主标签（155行）
│       └── 参数化扫描-所有探针.csv     # 探针数据
│
└── 参数化扫描2/
    └── 同上，155张样本
```

### 3.3 多批次数据统计

| 批次 | 样本数 | 时间范围 | 说明 |
|------|--------|----------|------|
| 单次扫描 | 21 | 0~1s（步长0.05s） | 单一参数仿真 |
| 参数化扫描1 | 155 | 5 h_ceramic × 31 steps | 参数化仿真 |
| 参数化扫描2 | 155 | 同上 | 额外参数化数据 |
| **总计** | **331** | - | 当前使用的有表格批次 |

**训练/测试划分**：
- 训练集：264 样本（80%）
- 测试集：67 样本（20%）

**扩展说明**：
- 若使用分割任务（`--task segmentation`），可加入无表格的批次（如参数化扫描3、4）
- 这些批次有 `d_hist/*.png` 损伤场图像，可直接作为分割标签
- 详见 [README.md](../README.md) 中的"分割任务"章节

### 3.4 CSV格式智能解析

`parse_label_csv()` 函数根据列数和统计特征自动识别格式：

**单次扫描格式（10列）**：
```
col0=time, col1=temp, col2=x, col3=y, col4=density, col5=hist_x...
```

**参数化扫描格式（11列）**：
```
col0=h_ceramic, col1=time, col2=temp, col3=x, col4=y, col5=density...
```

**识别逻辑**：
1. 列数 = 11 且 col0 均值 > 100 → 参数化格式
2. 列数 = 10 → 单次扫描格式
3. 其他情况：基于统计特征推断

### 3.5 图像预处理流程

```
PNG (640×480, RGB)
       │
       ▼ [灰度转换]
灰度图 (640×480)
       │
       ▼ [Max-Min归一化]
归一化图 (0~1)
       │
       ▼ [Center Crop 70%]
裁剪图 (448×336)
       │
       ▼ [Resize 256×256]
256×256 灰度Tensor [0,1]
       │
       ▼ [通道拼接]
2×256×256 Tensor (温度场 + 应力场)
```

**为什么用 Max-Min 归一化？**
- 不同批次数据的色标/颜色映射可能不同
- Max-Min 归一化将图像值域映射到 [0,1]，消除绝对值差异
- Center Crop 去除边缘留白，聚焦物理区域（10mm×10mm）

### 3.6 1D 时序插值流程

```
Table_Temp.csv (706点)
       │
       ▼ [按h_ceramic分组]
每个h值对应的时间序列
       │
       ▼ [对齐到21个参考时间点]
取3个探针点温度均值
       │
       ▼ [interpolate_seq]
300点序列
```

---

## 5. 模型架构详解

### 4.1 整体结构

```
输入
  ├── x_1d: (batch, 300)  ──────────────────────────┐
  │                                              ↓
  │                                    TemporalFeatureExtractor
  │                                         (Micro+Macro 1D-CNN
  │                                          + Self-Attention)
  │                                              ↓
  └── x_2d: (batch, 2, 256, 256) ────────→ ResNet18Backbone2D
                                                (去掉最后的 FC)
                                                     ↓
                                              feat_2d: (batch, 512)
                                                     ↓
                                          CrossAttentionFusion
                                          (双向交叉注意力)
                                                     ↓
                                              concat: (batch, 576)
                                                     ↓
                                    ┌────────────────┴────────────────┐
                                    ↓                                  ↓
                              MultiTaskHead                      MaskDecoder
                              (检测输出头)                        (分割输出头)
                                    ↓                                  ↓
                         [x, y, l, w, conf, density]      (batch, 1, 256, 256)
                                    ↓
                              (根据 task 参数选择输出)

根据 --task 参数：
├── detection: 返回 MultiTaskHead 输出 (batch, 6)
├── segmentation: 返回 MaskDecoder 输出 (batch, 1, image_size, image_size)
└── multitask: 返回 (MaskDecoder输出, MultiTaskHead输出) 元组
```

### 4.2 各模块详细说明

#### ResNet18Backbone2D（2D 视觉骨干）

```python
# 输入：(batch, 2, 256, 256) —— 2 通道（温度+应力）
# 出口：(batch, 512) —— 512 维特征向量

# ImageNet 预训练的 3 通道权重需要适配到 2 通道输入
# 方法：将第一个卷积层的 weight 平均到 3 通道，然后复制到 2 通道
```

#### Micro1DCNN（微观特征提取）

```python
# 设计意图：捕捉瞬态温度跳变（高频成分）
# kernel_size=3, dilation=1：感受野仅 3 个时间点
# 两层：Conv1d(1→32) → BN → ReLU → Conv1d(32→32) → BN → ReLU
# 输出：(batch, 32, 300)
```

#### Macro1DCNN（宏观特征提取）

```python
# 设计意图：提取长程热耗散趋势（低频成分）
# kernel_size=7, dilation=3, padding=9：有效感受野约 19 个时间点
# 两层：Conv1d(1→32) → BN → ReLU → Conv1d(32→32) → BN → ReLU
# 输出：(batch, 32, 300)
```

#### TemporalFeatureExtractor（时序融合）

```python
# Micro + Macro 并行 → Concat → 1×1 Conv 融合 → LayerNorm → MultiHead Self-Attention → FFN → GlobalAvgPool
# 输出：(batch, 64)
```

#### CrossAttentionFusion（跨模态融合）

**双向交叉注意力的工作方式**：

```python
# 方向1：图像查询温度（2D 作为 Query，1D 作为 Key/Value）
attn_2d_to_1d = softmax(Q_2d @ K_1d.T / sqrt(d_k)) @ V_1d

# 方向2：温度查询图像（1D 作为 Query，2D 作为 Key/Value）
attn_1d_to_2d = softmax(Q_1d @ K_2d.T / sqrt(d_k)) @ V_2d

# 融合：拼接两个方向的输出
fused = concat(attn_2d_to_1d, attn_1d_to_2d, dim=-1)  # (batch, 576)
```

#### MultiTaskHead（多任务输出头）

```python
# 输入：(batch, 576)
# 结构：Linear(576→256) → ReLU → Dropout(0.2) → Linear(256→6)
#
# 坐标损失：[x, y, l, w] → ReLU（确保非负）
# 分类损失：[confidence, density] → Sigmoid（映射到 [0,1]）
```

---

## 6. 损失函数详解

### 5.1 任务模式与损失函数选择

根据 `--task` 参数，系统自动选择对应的损失函数：

| task 参数 | 损失函数 | 说明 |
|-----------|----------|------|
| `detection` | `MultimodalCrackLoss` | MSE + 单调性 + DIoU + BCE |
| `segmentation` | `SegmentationLoss` | Dice Loss + BCE |
| `multitask` | `MultimodalSegmentationLoss` | Dice + BCE + 检测损失 |

### 5.2 Detection 损失公式

```
L_total = λ_mse · L_mse_density + λ_mono · L_mono + λ_loc · L_loc + λ_conf · L_conf
```

默认值：λ_mse=1.0, λ_mono=0.1, λ_loc=1.0, λ_conf=1.0

### 5.3 Segmentation 损失公式

```
L_total = λ_dice · L_dice + λ_bce · L_bce
```

默认值：λ_dice=1.0, λ_bce=0.5

**Dice Loss 原理**：
```
Dice = 2 * |A ∩ B| / (|A| + |B|)
L_dice = 1 - Dice
```
对类别不平衡更鲁棒，裂纹区域通常只占图像的一小部分。

### 5.4 各分量物理意义

#### L_mse_density（MSE 损失）

```python
L_mse = MSELoss(pred_density, target_density)
```

最简单的回归损失，衡量密度预测值与真实值的均方误差。

#### L_mono（单调性损失）

**物理原理**：格里菲斯断裂准则指出，裂纹一旦萌生，在持续热应力作用下只会扩展、不会愈合。因此裂纹密度随时间单调递增。

**实现**（成对排序损失）：
```python
# 样本按时间排序后，相邻样本对比较
for i in range(n - 1):
    loss += max(0, pred[i+1] - pred[i])
L_mono = sum / (n - 1)
```

#### L_loc（定位损失）

```python
L_loc = DIoULoss(pred_boxes, target_boxes) + 0.5 * MSE(pred_boxes, target_boxes)
```

#### L_conf（置信度损失）

```python
# 真实标签：density > 0.5 → confidence=1，否则 0
target_conf = (density > 0.5).float()
L_conf = BCE(pred_conf, target_conf)
```

---

## 7. 训练流程详解

### 7.0 统一启动器 (`train_launcher.py`) ⭐推荐

推荐使用统一启动器进行训练：

```bash
# 单次训练 - 交互式选择预设
python train_launcher.py

# 批量训练 - 交互式
python train_launcher.py --batch

# 快捷批量训练
python train_launcher.py --quick "resnet18,100,0,detection" "vit_small,100,0,detection"
```

**功能**：
- 预设模板快速启动
- 交互式自定义配置
- 消融实验预设
- 批量训练 + 结果对比

### 7.1 底层训练脚本 (`run_train.py`)

直接调用 `run_train.py` 进行训练：

```bash
# 训练单模型
python run_train.py --mode train --variant full --epochs 150

# 消融实验（6个变体）
python run_train.py --mode ablation

# 评估检查点
python run_train.py --mode eval --checkpoint ./checkpoints/xxx.pt
```

支持的模型变体：
- `1d_only`：仅时序分支
- `2d_only`：仅空间分支
- `concat`：双分支+拼接
- `add`：双分支+加法
- `cross_attn`：Cross-Attention
- `full`：完整MM-DBFNet

### 7.2 训练流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 数据加载                                                 │
│    create_multibatch_dataloaders(data_roots, batch_size=16) │
│    → train_loader (264 样本) / test_loader (67 样本)        │
│    数据路径从 config.json 自动读取                          │
├─────────────────────────────────────────────────────────────┤
│ 2. 模型初始化                                               │
│    PETSNetMultimodal(pretrained_2d=True)                    │
│    → 约 12M 参数                                            │
├─────────────────────────────────────────────────────────────┤
│ 3. 损失函数与优化器                                         │
│    MultimodalCrackLoss(λ_mse=1, λ_mono=0.1, λ_loc=1, λ_conf=1)│
│    AdamW(lr=3e-4, weight_decay=0.01)                        │
│    CosineAnnealingLR(T_max=150, eta_min=1e-6)               │
├─────────────────────────────────────────────────────────────┤
│ 4. 早停机制                                                 │
│    patience=20：验证损失连续 20 个 epoch 不下降则停止        │
├─────────────────────────────────────────────────────────────┤
│ 5. Epoch 循环                                               │
│    for epoch in range(1, 151):                              │
│        train_one_epoch → val_one_epoch → 记录指标            │
│        保存 best_model.pt（验证损失最低的检查点）             │
│        早停计数达到 patience → break                         │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 训练命令参考

```bash
# 通过统一启动器（推荐）
python train_launcher.py              # 交互式单次训练
python train_launcher.py --batch      # 交互式批量训练
python train_launcher.py --quick "resnet18,100,0,detection"  # 快捷批量

# 直接训练
python run_train.py --mode train --variant full  # 训练完整模型（默认150 epoch）

# 训练并指定参数
python run_train.py --mode train --epochs 100 --lr 1e-4

# 显存自适应（自动检测显存选择配置）
python run_train.py --mode train                    # 自动选择（默认512+FP16）
python run_train.py --mode train --batch_size 16    # 指定batch_size，image_size自动

# 高分辨率训练（支持 256/384/512/768/1024）
python run_train.py --mode train --image_size 512   # 512x512 高分辨率
python run_train.py --mode train --image_size 384   # 384x384 中分辨率

# FP16 控制
python run_train.py --mode train --fp16             # 启用FP16（默认）
python run_train.py --mode train --no_fp16          # 禁用FP16

# 评估检查点（注意：评估时也需要指定相同的 image_size）
python run_train.py --mode eval --checkpoint ./checkpoints/xxx.pt --image_size 512

# 运行消融实验
python run_train.py --mode ablation
```

### 6.3 显存自适应配置

系统会根据可用显存自动选择最佳配置：

| 空闲显存 | image_size | batch_size | fp16 |
|----------|------------|------------|------|
| ≥10 GB | 512 | 16 | True |
| 6-10 GB | 512 | 8 | True |
| 4-6 GB | 384 | 8 | True |
| <4 GB | 256 | 4 | False |

**特点**：
- 用户指定任一参数 → 该参数不自动降级
- 训练前执行干跑验证，提前触发 OOM
- 训练中 OOM 时打印建议并抛出特定异常
- 实际配置记录在检查点元数据中（`final_image_size`, `final_batch_size`, `fp16_enabled`）

### 6.3 旧脚本（已废弃）

以下脚本已移至 `old_code/` 目录，**不建议使用**：
- `main_v2.py`：单数据集训练入口
- `multi_dataset_trainer.py`：旧版多批次训练脚本

如需使用，请参考 `run_train.py` 的实现方式。

---

## 8. 数据增强详解

### 7.1 物理安全约束

**绝对禁止的增强**（破坏物理自洽性）：
- 水平/垂直翻转（温度场/应力场有物理方向性）
- 随机旋转（裂纹萌生位置有方向偏好）
- 随机裁剪/缩放（改变温度梯度的空间分布）

**允许的增强**（模拟传感器误差，不破坏物理规律）：
- 极微小高斯噪声（模拟热电偶测量噪声）
- 极微小亮度/对比度调整（模拟光照变化）
- 时间遮蔽（Time Masking，模拟数据采集瞬时缺失）

### 7.2 图像预处理 vs 数据增强

| 阶段 | 处理 | 说明 |
|------|------|------|
| 预处理 | ImagePreprocessor | 所有数据必经：灰度、Max-Min归一化、CenterCrop、Resize |
| 增强 | PhysicalSafeTransform2D | 仅训练集：可选的噪声/亮度调整 |

---

## 9. 如何添加新数据批次

### 8.1 目录结构要求

新批次目录需包含：
```
新批次名称/
├── 温度/           # 温度场图像，命名为 XXX01.png ~ XXXNN.png
├── 应力/           # 应力场图像，与温度文件一一对应
├── d/              # 损伤场图像（可选）
└── 表格/
    ├── Table_表面最大值.csv    # 主标签文件
    └── Table_Temp.csv          # 探针数据（或等效文件）
```

### 8.2 CSV格式要求

系统支持两种CSV格式：

**格式1：单次扫描格式**
```
time,temp,x,y,density,...
0,1431.19,0.00988,0.00999,0.1057,...
```

**格式2：参数化扫描格式**
```
h_ceramic,time,temp,x,y,density,...
1000,0,1431.15,0.00006,0.00999,0.186,...
```

### 8.3 验证新批次

```python
from data.dataset_multimodal import create_multibatch_dataloaders
from utils.config import get_data_root
import os

data_root = get_data_root()
new_batch_path = os.path.join(data_root, "新批次名称")

train_loader, test_loader = create_multibatch_dataloaders(
    data_roots=[new_batch_path],
    batch_size=8
)
print(f"训练样本: {len(train_loader.dataset)}")
print(f"测试样本: {len(test_loader.dataset)}")
```

---

## 10. 常见问题与排查

### Q1：训练 loss 一直是 NaN？

**可能原因**：
1. 学习率过高 → 降低 `--lr`（尝试 1e-4）
2. 数据中存在 NaN → 检查 CSV 插值是否正确
3. 损失权重不平衡 → 优先确保 λ_mono 不要过大

**排查方法**：
```bash
python run_train.py --mode train --lr 1e-4
```

### Q2：某个批次加载失败？

**可能原因**：
1. CSV 文件名不匹配 → 检查 `parse_label_csv` 支持的文件名列表
2. 图像文件缺失 → 确认温度/应力/d目录下的文件数量匹配

**排查方法**：
```python
from data.dataset_multimodal import load_csv_safely, parse_label_csv

df = load_csv_safely("D:/path/to/表格/xxx.csv")
data = parse_label_csv(df)
print(f"样本数: {len(data['time'])}")
```

### Q3：早停太早？

**可能原因**：
1. 学习率过高
2. 数据增强过强
3. patience 值过小

**排查方法**：在 `run_train.py` 中调整 `--patience` 参数或降低学习率

### Q4：如何判断模型是否收敛？

**观察指标**：
1. 验证损失持续下降
2. 单调性得分 > 0.8
3. R² > 0.5

---

## 11. 代码修改指南

### 10.1 如何添加新的损失分量？

1. 在 `training/mono_loss.py` 中定义新的 Loss 类
2. 在对应的损失组合类中添加权重参数和损失类实例
3. 在 `forward` 方法中调用新损失类并加入总损失
4. 在 `run_train.py` 的 `train_model()` 中添加对应的任务类型分支

**示例：添加新的分割损失分量**

```python
# 1. 在 mono_loss.py 中定义
class FocalLoss(nn.Module):
    def __init__(self, alpha=0.25, gamma=2.0):
        super().__init__()
        self.alpha = alpha
        self.gamma = gamma

    def forward(self, pred, target):
        # Focal Loss 实现
        ...

# 2. 在 SegmentationLoss 中集成
class SegmentationLoss(nn.Module):
    def __init__(self, lambda_dice=1.0, lambda_bce=0.5, lambda_focal=0.3):
        super().__init__()
        self.dice_loss = DiceLoss()
        self.bce_loss = nn.BCELoss()
        self.focal_loss = FocalLoss()
        self.lambda_dice = lambda_dice
        self.lambda_bce = lambda_bce
        self.lambda_focal = lambda_focal

    def forward(self, pred, target):
        return (self.lambda_dice * self.dice_loss(pred, target) +
                self.lambda_bce * self.bce_loss(pred, target) +
                self.lambda_focal * self.focal_loss(pred, target))
```

### 10.2 如何替换 2D 骨干网络？

修改 `models/pe_tsnet_multimodal.py` 中的 `ResNet18Backbone2D`：
- 保持 `forward(x)` 返回 `(batch, 512)` 即可

### 10.3 如何导出 ONNX 用于边缘部署？

```python
import torch.onnx

model = PETSNetMultimodal()
model.load_state_dict(torch.load('checkpoints/best_model.pt'))
model.eval()

# 导出
dummy_1d = torch.randn(1, 300)
dummy_2d = torch.randn(1, 2, 256, 256)
torch.onnx.export(
    model, (dummy_1d, dummy_2d),
    "pe_mmnet.onnx",
    input_names=['x_1d', 'x_2d'],
    output_names=['output'],
    dynamic_axes={'x_1d': {0: 'batch'}, 'x_2d': {0: 'batch'}, 'output': {0: 'batch'}}
)
```

---

## 12. 统一训练启动器

### 12.1 入口点

PE-MMNet v4 提供统一的训练启动器 `train_launcher.py`：

```bash
# 单次训练（交互式）
python train_launcher.py

# 批量训练（交互式）
python train_launcher.py --batch

# 快捷批量训练
python train_launcher.py --quick "resnet18,100,0,detection" "vit_small,100,0,detection"
```

### 12.2 核心模块

| 模块 | 文件 | 功能 |
|------|------|------|
| 统一启动器 | `train_launcher.py` | 单次/批量训练统一入口 |
| 批量训练GUI | `tools/batch_train_gui.py` | 交互式批量训练界面 |
| 单次训练逻辑 | `launcher.py` | 预设模板、消融实验 |

### 12.3 批量训练核心类

| 类 | 文件 | 功能 |
|---|------|------|
| `TrainingCommand` | tools/batch_train_gui.py | 命令封装，包含所有训练参数 |
| `BatchTrainer` | tools/batch_train_gui.py | 批量训练控制器 |
| `ResultsComparator` | tools/batch_train_gui.py | 结果比较器 |
| `InteractiveCLI` | tools/batch_train_gui.py | 交互式界面 |

### 12.4 命令解析

```python
# 快捷格式
TrainingCommand.from_quick_format("resnet18,100,0,detection")
# → variant=full, backbone_2d=resnet18, epochs=100, offset=0, task=detection

# 完整命令解析
TrainingCommand.from_string("python run_train.py --mode train --variant full --epochs 100")
```

### 12.5 评估输出

训练完成后自动输出 JSON 行供解析：
```
__EVAL_JSON__{"r2": 0.8785, "rmse": 0.0499, ...}__EVAL_JSON__
```

### 12.6 检查点命名

```
{backbone_2d}_{backbone_1d}_{fusion}_task{task}_offset{offset}_best.pt
```

示例：`resnet18_cnn_attn_cross_attn_taskdetection_offset0_best.pt`

---

## 13. 文件清单与关键行号

| 文件 | 关键类/函数 | 关键行号 |
|------|-------------|----------|
| `utils/config.py` | `load_config` | 配置加载 |
| | `get_data_root` | 获取数据根目录 |
| | `get_data_batches` | 获取数据批次列表 |
| | `ensure_config` | 交互式配置 |
| `utils/console.py` | `print_title` | 标题打印 |
| | `print_result` | 结果打印 |
| | `print_results_table` | 表格打印 |
| `data/dataset_multimodal.py` | `ImagePreprocessor` | 图像预处理 |
| | `MaskLabelProcessor` | 分割标签处理 |
| | `SingleBatchDataset` | 单批次数据集 |
| | `MultiBatchCollateDataset` | 多批次数据集 |
| | `create_multibatch_dataloaders` | 数据加载器工厂 |
| | `parse_label_csv` | CSV解析 |
| | `interpolate_seq` | 序列插值 |
| `models/pe_tsnet_multimodal.py` | `PETSNetMultimodal` | 主模型 |
| | `ResNet18Backbone2D` | 2D骨干网络 |
| | `CrossAttentionFusion` | 跨模态融合 |
| | `MultiTaskHead` | 检测输出头 |
| | `MaskDecoder` | 分割输出头 |
| `training/mono_loss.py` | `MultimodalCrackLoss` | 检测损失 |
| | `SegmentationLoss` | 分割损失 |
| | `MultimodalSegmentationLoss` | 多任务损失 |
| | `DiceLoss` | Dice损失 |
| | `MonotonicityLossV3` | 单调性损失 |
| | `diou_loss` | DIoU损失 |
| `run_train.py` | `train_model` | 模型训练 |
| | `train_variant` | 消融变体训练 |
| | `evaluate_model` | 模型评估 |
| | `eval_checkpoint` | 检查点评估 |
| `train_launcher.py` | `main` | 统一训练入口 |
| `launcher.py` | `interactive_mode` | 交互式配置 |
| `tools/batch_train_gui.py` | `TrainingCommand` | 命令封装 |
| | `BatchTrainer` | 批量训练控制器 |

---

## 14. 参考资料

- COMSOL 仿真数据：config.json 中的 `data_root` 路径
- v2/v3 历史版本：project_v2/project_v3 目录
- 设计文档：v4设计文档.txt
